# Redux Slices

The client-state architecture, including `AsyncState<T>`, async thunks, client
services, hooks, and exhaustive render pipelines, is documented in the
[store architecture README](../README.md).

Slice directories contain the state and async operations owned by their
respective client domains.
